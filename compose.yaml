services:
  api:
    build: .
    platform: linux/amd64
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      ORT_INTRA_THREADS: "2"
      CORS_ORIGINS: "*"
    mem_limit: 2g
    cpus: 2
    restart: unless-stopped
